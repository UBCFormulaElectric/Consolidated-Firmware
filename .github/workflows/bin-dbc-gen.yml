name: Binary and DBC Generation
on:
  push:
    branches:
      - master
      - edwinZ/bin-dbc-gen
jobs:
  build:
    uses: UBCFormulaElectric/Consolidated-Firmware/.github/workflows/build.yml@edwinZ/bin-dbc-gen
    secrets:
      docker_token: ${{secrets.DOCKER_TOKEN}}
    with:
      make-release: true