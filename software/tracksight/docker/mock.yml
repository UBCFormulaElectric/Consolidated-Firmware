name: tracksight-mock
services:
  influx:
    extends:
      file: common.yml
      service: influx
  live_data:
    extends:
      file: common.yml
      service: live_data
  # frontend:
  #   extends:
  #     file: common.yml
  #     service: frontend
