name: tracksight-wireless
services:
  influx:
    extends:
      file: ../common.yml
      service: influx
  backend:
    extends:
      file: ../common.yml
      service: backend
volumes:
  influx-data:
  influx-config:
