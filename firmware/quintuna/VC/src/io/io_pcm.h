#include "hw_gpio.h"

// TODO: do we need this file? Or can we just enable/disable the GPIO directly

/**
 * Enable the PCM.
 * @param enable Whether or not to enable the PCM.
 */
void pcm_set(bool enable);
