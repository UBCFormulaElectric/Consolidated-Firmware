[project]
name = "chimera_v2"
version = "0.1"

# We use libusb-package to get a version of pyusb's usb.core with libusb packaged in.
dependencies = ["grpcio-tools", "protoletariat", "libusb-package"]

[tool.setuptools]
py-modules=["chimera_v2"]
packages=["proto_autogen"]
