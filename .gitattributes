# Make default behaviour CRLF line ending because most files are Windows-based
* text eol=crlf

# Force LF line ending for Linux-specific files
*.sh text eol=lf

# Denote all files that are truly binary and should not be modified
clang-format/clang-format-4.0 binary
*.exe binary
