// 
// Fakes for: {{ name }}
//

/**
 * Reset the fake for {{ name }}.
 */
void fake_{{ name }}_reset(void);

/**
 * Return the number of times {{ name }} has been invoked.
 */
uint32_t fake_{{ name }}_callCount(void);

/**
 * Return the number of times {{ name }} has been invoked with a specific set of arguments.
 */
uint32_t fake_{{ name }}_callCountForArgs(
{%- for param in params %}
    {{ param.type }} {{ param.name }}
    {%- if not loop.last %}, {% endif %}
{%- endfor %}
);

/**
 * Set a callback function to be called when {{ name }} is invoked.
 */
void fake_{{ name }}_setCallBack(void (*func_ptr)({%- for param in params %}
    {{ param.type }}
    {%- if not loop.last %}, {% endif %}
{%- endfor %}));
