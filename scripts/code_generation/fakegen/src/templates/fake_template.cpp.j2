#include "fake_{{ module }}.hpp"
#include <string.h>
#include <unordered_map>

extern "C"
{
#include "{{ module }}.h"
}

using namespace std;

{% for definition in definitions -%}

{{ definition }}

{% endfor %}
