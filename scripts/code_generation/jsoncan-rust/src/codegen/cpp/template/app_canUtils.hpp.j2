/**
 * @note This file is auto-generated. Do not modify!
 */
// clang-format off

#pragma once

/* ------------------------------- Includes ------------------------------- */

#include <cstdint>
#include <span>
#include <array>

/* -------------------------------- Structs --------------------------------- */
/**
 * Standard CAN msg type.
 */
struct JsonCanMsg
{
    using data_array = std::array<uint8_t, {% if fd -%}64{%- else -%}8{%- endif -%}>;

    uint32_t std_id;
    uint32_t dlc;
    data_array data{};

    JsonCanMsg(const uint32_t id, const uint32_t _dlc, const data_array _data) : std_id(id), dlc(_dlc), data(_data) {}
};

namespace app::can_utils {
/* -------------------------------- Enums --------------------------------- */
{%- for e in enums %}
enum class {{ e.name }}
{
    {%- for (key, value) in e.values %}
    {{ key }} = {{ value }},
    {%- endfor %}
};
{%- endfor %}

enum class CanNode
{
    {%- for node_name in node_names %}
    {{node_name.to_uppercase()}}_NODE = {{loop.index}},
    {%- endfor %}
};

// BUS ENUMS
enum class BusEnum
{
    {%- for bus in node_buses %}
    Bus_{{bus.name}} = {{loop.index0}},
    {%- endfor %}
};

// MODE ENUMS
{%- for bus in node_buses %}
enum class {{bus.name}}Mode
{
    {%- for mode in bus.modes %}
    {{bus.name.to_uppercase()}}_MODE_{{mode}} = 1 << {{loop.index0}},
    {%- endfor %}
};
{% endfor %}

/* ------------------------------- Structs -------------------------------- */

{# THESE ARE THE SIGNAL STRUCTS #}
{% for msg in messages %}
struct {{ msg.name }}_Signals
{
    {%- if msg.signals.len() == 0 %}
    uint8_t _placeholder;
    {%- else %}
    {%- for signal in msg.signals %}
    {{ signal.datatype() }} {{ signal.name }}_value = {{ signal.start_val_macro()}};
    {%- endfor %}
    explicit {{ msg.name }}_Signals(std::span<const uint8_t> data);
    explicit {{ msg.name }}_Signals(
        {%- for signal in msg.signals -%}
        const {{ signal.datatype() }} {{ signal.name }}_value_arg{%- if !loop.last %}, {% endif %}
        {%- endfor %}
    ) :
    {%- for signal in msg.signals -%}
    {{ signal.name }}_value({{ signal.name }}_value_arg){% if !loop.last %}, {% endif %}
    {%- endfor %}
    {}
    {%- endif %}
    {{ msg.name }}_Signals() = default;

    void pack(std::span<uint8_t> out_data) const;

    // Msg IDs
    static constexpr uint32_t MSG_ID = {{ msg.id }};

    // Msg lengths (in bytes)
    static constexpr uint8_t MSG_DLC = {{ msg.dlc() }};

    // Msg cycle times (in MS)
    {%- if msg.cycle_time.is_some() %}
    static constexpr uint32_t MSG_CYCLE_TIME = {{ msg.cycle_time.unwrap() }};
    {%- endif -%}

    // Signal Starting Values
    {% for signal in msg.signals -%}
    static constexpr {{ signal.datatype() }} {{ signal.start_val_macro() }} = {{ signal.start_val_name() | safe }};
    {%- if signal.unit.is_some() && signal.unit.as_ref().unwrap() != "" -%}// {{ signal.unit.as_ref().unwrap() }} {%- endif %}
    {% endfor %}

    // Scale/offset values for encoding/decoding signals
    {% for signal in msg.signals -%}
    static constexpr {{ signal.offset_scale_datatype() }} {{ signal.scale_macro() }} = {{ signal.scale_val_name() | safe }};
    // Note: unit may be optional
    {%- if signal.unit.is_some() && signal.unit.as_ref().unwrap() != "" -%}// {{ signal.unit.as_ref().unwrap() }} {%- endif %}
    static constexpr {{ signal.offset_scale_datatype() }} {{ signal.offset_macro() }} = {{ signal.offset_val_name() | safe }};
    {%- if signal.unit.is_some() && signal.unit.as_ref().unwrap() != "" -%}// {{ signal.unit.as_ref().unwrap() }} {%- endif %}
    {% endfor %}

    // Min/max allowed values for signals
    {% for signal in msg.signals -%}
    static constexpr {{ signal.datatype() }} {{ signal.min_val_macro() }} = {{ signal.min_val_name() | safe }};
    // Only print unit if present and non-empty
    {%- if signal.unit.is_some() && signal.unit.as_ref().unwrap() != "" -%}// {{ signal.unit.as_ref().unwrap() }} {%- endif %}
    static constexpr {{ signal.datatype() }} {{ signal.max_val_macro() }} = {{ signal.max_val_name() | safe }};
    {%- if signal.unit.is_some() && signal.unit.as_ref().unwrap() != "" -%}// {{ signal.unit.as_ref().unwrap() }} {%- endif %}
    {% endfor %}
};
{% endfor %}
}
