/**
 * @note This file is auto-generated. Do not modify!
 */
// clang-format off

/* ------------------------------- Includes ------------------------------- */

#include "io_canRx.hpp"
#include "app_canRx.hpp"
#include "app_canUtils.hpp"

namespace io::can_rx {
/* ------------------------- Function Definitions ------------------------- */
// each bus has its own filter function
{% for (bus_name, messages) in messages_on_busses -%}
bool filterMessageId_{{bus_name}}(uint32_t std_id)
{
    switch (std_id)
    {
        {%- if messages.len() > 0 %}
        {%- for msg in messages %}
        case {{msg.id_macro()}}:
        {%- endfor %}
            return true;
        {%- endif %}
        default:
        {
            // Do nothing
            break;
        }
    }
    return false;
}
{% endfor %}


void updateRxTableWithMessage(JsonCanMsg* msg)
{
    switch (msg->std_id)
    {
        {%- for (bus_name, messages) in messages_on_busses %}
        {%- for msg in messages %}
        case {{msg.id_macro()}}:
        {
            {{msg.name}}_Signals out_msg;
            app::can_utils::unpack(msg->data, &out_msg);
            
            {%- for signal in msg.signals %}
            app::can_rx::update_{{signal.name}}(out_msg.{{signal.name}}_value);
            {%- endfor %}
            break;
        }
        {%- endfor %}
        {%- endfor %}
        default:
        {
            // Do nothing
            break;
        }
    }
}
}