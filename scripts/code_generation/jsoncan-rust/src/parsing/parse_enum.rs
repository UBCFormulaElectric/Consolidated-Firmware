use crate::can_database::CanEnum;
use std::collections::HashMap;

pub fn parse_shared_enums(can_data_dir: &String) -> HashMap<String, CanEnum> {
    // Placeholder function to parse shared enums
    // Returns empty HashMap
    HashMap::new()
}

pub fn parse_node_enum_data(can_data_dir: &String, node_name: &String) -> HashMap<String, CanEnum> {
    // Placeholder function to parse node-specific enums
    // Returns empty HashMap
    HashMap::new()
}
