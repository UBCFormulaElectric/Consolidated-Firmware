/**
 * @note This file is auto-generated. Do not modify!
 */
// clang-format off

/* ------------------------------- Includes ------------------------------- */

#include "io_canTx.h"
#include <string.h>
#include "app_canTx.h"
#include "app_canUtils.h"

#ifndef THREAD_SAFE_CAN_PACKING
#include <FreeRTOS.h>
#include <portmacro.h>
#endif

/* --------------------------- Static Variables --------------------------- */
{% for bus in buses %}
static uint32_t can_mode_{{bus.name}};
static void (*transmit_func_{{bus.name}})(const JsonCanMsg* tx_msg);
{% endfor %}

/* --------------------- Static Function Definitions ---------------------- */

{% for msg in messages %}
{%- if msg.cycle_time != None -%}
static void io_canTx_{{msg.name}}_sendPeriodic()
{%- else -%}
staic void io_canTx_{{msg.name}}_sendAperiodic()
{%- endif %}
{
    JsonCanMsg tx_msg;
    memset(&tx_msg, 0, sizeof(JsonCanMsg));
    tx_msg.std_id = {{msg.id_macro()}};
    tx_msg.dlc = {{msg.bytes_macro()}};
    app_canUtils_{{msg.name}}_pack(app_canTx_{{msg.name}}_getData(), tx_msg.data);

    {% for bus in buses %}
    if (can_mode_{{bus.name}} & (CanMode_{{bus.name}}_{{bus.default_mode}}))
    {
        transmit_func_{{bus.name}}(&tx_msg);
    }
    {% endfor %}
}
{% endfor %}


/* --------------------- Public Function Definitions ---------------------- */

void io_canTx_init( 
    {% for bus in buses -%}
    void (*transmit_{{bus.name}}_msg_func)(const JsonCanMsg*) {%- if not loop.last -%}, {%- endif %}
    {% endfor %}
)
{
    {% for bus in buses -%}
    transmit_func_{{bus.name}} = transmit_{{bus.name}}_msg_func;
    {% endfor %}
}

{% for bus in buses %}
void io_canTx_enableMode_{{bus.name}}(CanMode_{{bus.name}} mode, bool enable)
{
    if (enable)
    {
        can_mode_{{bus.name}} |= (uint32_t)mode; // Enable mode
        
    }
    else
    {
        can_mode_{{bus.name}} &= ~((uint32_t)mode); // Disable mode
    }
}
{% endfor %}



void io_canTx_enqueue1HzMsgs()
{
    {%- for msg in messages %}
    {%- if msg.cycle_time == 1000 %}
    io_canTx_{{msg.name}}_sendPeriodic();
    {%- endif -%}
    {% endfor %}
}

void io_canTx_enqueue100HzMsgs()
{
    {%- for msg in messages -%}
    {%- if msg.cycle_time == 10 %}
    io_canTx_{{msg.name}}_sendPeriodic();
    {%- endif -%}
    {% endfor %}
}

void io_canTx_enqueueOtherPeriodicMsgs(uint32_t time_ms)
{
    {% for msg in messages -%}
    {%- if msg.cycle_time != 1000 and msg.cycle_time != 10 and msg.cycle_time != None -%}
    if (time_ms % {{msg.cycle_time_macro()}} == 0)
    {
        io_canTx_BMS_Vitals_sendPeriodic();
    }
    {%- endif %}
    {% endfor %}
}

