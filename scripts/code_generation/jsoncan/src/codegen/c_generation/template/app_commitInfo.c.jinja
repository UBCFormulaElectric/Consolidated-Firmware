#include "app_commitInfo.h"
/* 
  This file is intentionally empty,
  it only exists to tell cmake to scan 

  and to make pedantic compilation happy (avoid empty translation unit error)
*/

enum commit_info { happy_pedantic_compilation };