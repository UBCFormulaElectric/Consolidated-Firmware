/**
 * @note This file is auto-generated. Do not modify!
 */
// clang-format off

#pragma once

/* ------------------------------- Includes ------------------------------- */

#include "app_canUtils.h"

/* ------------------------- Function Prototypes -------------------------- */

/**
 * Initialize RX signals to their starting values.
 */
void app_canRx_init(void);

/* --------- UPDATE --------- */
{% for msg in messages %}
{%- for signal in msg.signals %}
/**
 * Update value stored in RX table of signal {{ signal.name }} in msg {{ msg.name }}.
 */
void app_canRx_{{ signal.name }}_update({{ signal.datatype() }} value);
{%- endfor %}
{%- endfor %}

/* --------- GET --------- */
{% for msg in messages %}
{%- for signal in msg.signals %}
/**
 * Get value stored in RX table of signal {{ signal.name }} in msg {{ msg.name }}.
 */
{{ signal.datatype() }} app_canRx_{{ signal.name }}_get(void);
{%- endfor %}
{%- endfor %}

void app_canRx_clear_board_rx_table(CanNode board);