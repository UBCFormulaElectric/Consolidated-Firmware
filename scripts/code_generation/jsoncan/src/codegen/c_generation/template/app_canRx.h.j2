/**
 * @note This file is auto-generated. Do not modify!
 */
// clang-format off

#pragma once

/* ------------------------------- Includes ------------------------------- */

#include "app_canUtils.h"

/* ------------------------- Function Prototypes -------------------------- */

/**
 * Initialize RX signals to their starting values.
 */
void app_canRx_init(void);

/**
 * Update value stored in RX table of signal BRUSA_IsConnected in msg BRUSA_Vitals.
 */
void app_canRx_BRUSA_IsConnected_update(bool value);

{% for msg in messages -%}
{%- for signal in msg.signals -%}
/**
 * Update value stored in RX table of signal {{ signal.name }} in msg {{ msg.name }}.
 */
void app_canRx_{{ msg.name }}_{{ signal.name }}_update({{ signal.datatype() }} value);
{%- endfor %}
{% endfor %}
